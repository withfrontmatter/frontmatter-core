---
export interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle = "Default subtitle" } = Astro.props;
---

<section>
  <h1>{title}</h1>
  {subtitle && <p>{subtitle}</p>}
</section>
