---
import Hero from "../components/Hero.astro";

export interface Props {
  title: string;
  subtitle?: string;
}

const { title = "Hello", subtitle = "A tiny demo" } = Astro.props;
---

<main>
  <Hero title={title} subtitle={subtitle} />
</main>
